/**
 * Provides a bunch of methods to identify whether a certain feature is present in a piece of data.
 */
public class Feature {

    /**
     * A method that checks to see the presence of non-space characters in a pattern that represents a 1.
     * The data is broken into a 4x4 grid, each grid space being 5 lines by 7 characters long,
     * @return an integer representing how many
     */
    public static int oneFeature(String data) {
        int count = 0;

        return count;
    }


}
